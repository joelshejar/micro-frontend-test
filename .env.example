# Host App Environment Variables

# NODE_ENV: Determines if we're in development or production
# Development: uses localhost:3001 for remote
# Production: uses KV-provided URL
NODE_ENV=development

# Cloudflare KV Namespace Binding
# This is configured in Cloudflare Pages dashboard, not in .env
# Variable name: REMOTE_URLS
# This binding allows the edge function to read remote URLs from KV

# Production URLs (for reference)
# HOST_URL=https://micro-frontend-test-7m9.pages.dev
# REMOTE_URL=https://micro-frontend-test-remote.pages.dev/remoteEntry.js
